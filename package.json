{
  "name": "mosquito-drone-lab",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Dependency-free browser simulation and local residual-policy learning laboratory",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "python tests/browser_smoke.py",
    "benchmark": "node scripts/benchmark.mjs",
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs"
  },
  "engines": { "node": ">=22" }
}
